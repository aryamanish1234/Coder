//for of loop is iterate over iterable over iterable objects(arrays , sets , maps , string )

//Syntax
for (element of iterable) {
    // body 
}

const students = ['John', 'Sara', 'Jack'];
for (let element of students) {

    console.log(element);
}